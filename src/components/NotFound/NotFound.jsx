import React from "react";

const NotFound = () => (
  <div
    style={{
      height: 100 + "vh",
      display: "flex",
      alignItems: "center",
      justifyContent: "center"
    }}
  >
    Sorry, that address is unknown!
  </div>
);

export default NotFound;
